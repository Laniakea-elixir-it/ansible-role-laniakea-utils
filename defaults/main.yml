---
# defaults file for ansible-role-laniakea-utils

install_docker: False
export_dir: '/export'
docker_config:  { "data-root": "{{ export_dir }}/docker_image", }

install_conda: False
miniconda_prefix: '{{ export_dir }}/miniconda'
miniconda_version: '4.6.14'

install_dev_packages: False

install_nginx: False

install_nextflow: False

install_knime: False

install_cwl: False
